@echo off
setlocal enabledelayedexpansion

set "klasor=C:\Senin\Klasor\Yolu"
echo. > liste.txt

for /R "%klasor%" %%f in (*.prc) do (
    set "line="
    set /p line=<"%%f"
    for %%a in (!line!) do set "last=%%a"
    echo !last! >> liste.txt
)
